import React from 'react';
import styles from '../../styles/indexStyles'

export default class PageDisplay extends React.Component {
  render() {
    return (
      <div className={styles.page.verticalAlign}>Page Displays</div>
    );
  }
}
